# versioned-md configuration for this repository
# This file is generated by `versioned-md create` and controls template updates.

template:
  name: "{{ name }}"
  description: "{{ description }}"
  org: "{{ org }}"
  author: "{{ author }}"
